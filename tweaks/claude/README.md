# Claude tweaks

This folder is ready for optional Claude Code additions. No Claude-specific
tweaks are currently included.

Keep additions here independent from Codex integrations, with shared code at
the individual tweak root and platform-specific code in `linux/` and `windows/`.
